<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hanhwa_tae.gulhan.user.query.mapper.UserMapper">
    <select id="findRankIdByRankName" resultType="com.hanhwa_tae.gulhan.user.command.domain.aggregate.Rank">
        SELECT rank_id FROM rank WHERE rank_name = #{ rankName }
    </select>

<!--    <resultMap type="com.hanhwa_tae.gulhan.user.command.domain.aggregate.User" id="Rank">-->
<!--        <result column="">-->
<!--    </resultMap>>-->

    <select id="findUserByUserId" resultType="com.hanhwa_tae.gulhan.user.command.domain.aggregate.User">
        SELECT
          a.user_no
        , a.user_id
        , a.password
        , b.rank_id as "rank.rank_id"
        , b.rank_name as "rank.rank_name"
        FROM user a
        JOIN rank b ON (a.rank_id=b.rank_id)
        WHERE user_id = #{ userId }
    </select>
    <select id="findUserByEmail" resultType="com.hanhwa_tae.gulhan.user.command.domain.aggregate.User">
        SELECT email FROM user WHERE email = #{ email }
    </select>
    <select id="findUserByPhone" resultType="com.hanhwa_tae.gulhan.user.command.domain.aggregate.User">
        SELECT
            b.phone as "user_info.phone"
        FROM user a
        JOIN user_info b ON (a.user_no = b.user_no)
        WHERE b.phone = #{ phone }
    </select>
    <select id="findUserInfoByUserId" resultType="com.hanhwa_tae.gulhan.user.query.dto.response.UserInfoResponse">
        SELECT
              a.user_id
            , a.username
            , a.email
            , a.gender
            , b.birth
            , b.phone
            , b.address
        FROM user a
        JOIN user_info b ON (a.user_no = b.user_no)
        WHERE a.user_id = #{ userId }
        AND a.is_deleted = 'N'
    </select>

    <select id = "findPointByUserNo" resultType = "int">
        SELECT
            point
        FROM user_info
        WHERE user_no = #{ userNo }
    </select>

    <select id="findRankByUserNo" resultType= "com.hanhwa_tae.gulhan.user.command.domain.aggregate.Rank">
        SELECT
            rank_id, rank_name, point_rate
        from rank
        where rank_id = (select rank_id
        from user
        where user_no = #{ userNo })
    </select>
</mapper>