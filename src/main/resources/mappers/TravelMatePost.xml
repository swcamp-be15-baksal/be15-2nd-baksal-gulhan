<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hanhwa_tae.gulhan.travelmatepost.query.mapper.TmpMapper">
    <select id="selectTravelMatePosts" resultType="com.hanhwa_tae.gulhan.travelmatepost.query.dto.response.TmpDTO">
        SELECT * FROM travel_mate_post WHERE is_deleted = 'N'
        <if test="title != null">
            AND title LIKE CONCAT('%', #{ title }, '%')
        </if>
        ORDER BY created_at DESC;
    </select>

    <select id="countPosts" resultType="long">
        SELECT COUNT(*)
        FROM travel_mate_post
        WHERE is_deleted = 'N'
        <if test="title != null">
            AND title LIKE CONCAT('%', #{ title }, '%')
        </if>
        ;
    </select>

    <select id="selectTmpByTravelMatePostId" resultType="com.hanhwa_tae.gulhan.travelmatepost.query.dto.response.TmpDTO">
        SELECT * FROM travel_mate_post WHERE is_deleted = 'N' AND travel_mate_post_id = #{ travelMatePostId };
    </select>
</mapper>