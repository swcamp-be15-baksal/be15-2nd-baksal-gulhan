<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hanhwa_tae.gulhan.notice.query.mapper.NoticeMapper">
    <select id="selectNotices" resultType="com.hanhwa_tae.gulhan.notice.query.dto.response.NoticeDTO">
        SELECT * FROM notice WHERE is_deleted = 'N'
        <if test="title != null">
            AND title LIKE CONCAT('%', #{ title }, '%')
        </if>
        ORDER BY created_at DESC
        LIMIT #{ limit } OFFSET #{ offset };
    </select>

    <select id="countPosts" resultType="long">
        SELECT COUNT(*) FROM travel_mate_post WHERE is_deleted = 'N'
        <if test="title != null">
            AND title LIKE CONCAT('%', #{ title }, '%')
        </if>
        ;
    </select>

    <select id="selectDetailNotice" resultType="com.hanhwa_tae.gulhan.notice.query.dto.response.NoticeDTO">
        SELECT * FROM notice WHERE is_deleted = 'N' AND notice_id = #{ noticeId };
    </select>
</mapper>